language: generic

sudo: required

service:
    - docker

before_install:
    - docker build -t samibel/test_react -f dockerfile.dev .
script:
  - docker run -e CI=true samibel/test_react npm run test 
  